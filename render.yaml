services:
  # ── Next.js frontend ────────────────────────────────────────────────────────
  - type: web
    name: sessionmap-web
    runtime: node
    region: frankfurt
    plan: starter
    buildCommand: NODE_ENV=development pnpm install --frozen-lockfile && pnpm --filter @sessionmap/types build && NODE_OPTIONS=--max-old-space-size=450 NODE_ENV=production pnpm --filter @sessionmap/web build
    startCommand: pnpm --filter @sessionmap/web start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_WS_URL
        sync: false  # set in dashboard: wss://sessionmap-server.onrender.com

  # ── Express / WebSocket backend ─────────────────────────────────────────────
  - type: web
    name: sessionmap-server
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: NODE_ENV=development pnpm install --frozen-lockfile && pnpm --filter @sessionmap/types build && pnpm --filter @sessionmap/server build
    startCommand: node apps/server/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: CLIENT_ORIGIN
        sync: false  # set in dashboard: https://sessionmap-web.onrender.com
